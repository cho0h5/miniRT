#include "object.h"
#include "vector3.h"

double	cylinder_distance(const t_vector3 pos, const t_vector3 ray, t_object_category *cylinder)
{
	(void)pos;
	(void)ray;
	(void)cylinder;
	return (DOUBLE_MAX);
}
