#include "object.h"
#include "vector3.h"

double	cylinder_distance(const t_vector3 pos, const t_vector3 ray, const t_cylinder *cylinder)
{
	(void)pos;
	(void)ray;
	(void)cylinder;
	return (DOUBLE_MAX);
}
