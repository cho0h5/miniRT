#include "object.h"
#include "vector3.h"

double	sphere_distance(const t_vector3 pos, const t_vector3 ray, const t_object_category *sphere)
{
	(void)pos;
	(void)ray;
	(void)sphere;
	return (DOUBLE_MAX);
}
