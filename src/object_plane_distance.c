#include "object.h"
#include "vector3.h"

double	plane_distance(const t_vector3 pos, const t_vector3 ray, const t_object_category *plane)
{
	(void)pos;
	(void)ray;
	(void)plane;
	return (DOUBLE_MAX);
}
